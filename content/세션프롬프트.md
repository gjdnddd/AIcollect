# 세션 프롬프트 모음

> **A** — 사용자가 매 세션 시작 시 붙여넣기  
> **B** — 작업별 규칙 (AI가 작업 유형 바뀔 때 직접 읽거나 사용자에게 요청)  
> **C** — 맥락 관리 (계획서·맥락노트·체크리스트 MD 운영)  
> **레벨 1** — 핵심 규칙 (AI가 이상할 때 언제든 던지기)  
> **레벨 2** — 리마인드 프롬프트 (대화 오래됐을 때 던지기)

---

## 레벨 1. 핵심 규칙 (언제든 던지기)

> AI가 컨펌 없이 진행하거나, 모델 추천 안 하거나, 범위 벗어나기 시작할 때 그냥 던진다.

```
[핵심 규칙]
- 진행 전 반드시 모델 추천 + "Sonnet, 진행할까요?" 컨펌
- 작업 유형이 바뀌면 해당 규칙 달라고 요청하거나 직접 읽기
- 한 번에 하나의 작업만, 범위 벗어난 수정 금지
```

---

## 레벨 2. 리마인드 프롬프트 (대화 오래됐을 때)

> 20~30턴 이상 됐거나, AI가 이상한 방향으로 가기 시작할 때 던진다.
> "현재 작업 목표" 한 줄은 사용자가 직접 채워서 던진다.

```
[리마인드]
지금까지 한 것 요약해줘.
그리고 아래 규칙 다시 지키면서 계속해:
- 진행 전 모델 추천 + "Sonnet, 진행할까요?" 컨펌
- 작업 유형 바뀌면 해당 규칙 확인 후 진행
- 범위 준수, 한 번에 하나의 작업만
현재 작업 목표: [여기에 한 줄 적기]
```

---

## A. 세션 시작 템플릿 (매 채팅창 붙여넣기)

```
[절대 규칙 — 항상 적용]
- 진행 전 항상 모델 추천 + 컨펌. 형식: "Sonnet, 진행할까요?"
- 컨펌 없이 바로 진행하지 않는다.
- 한 번에 하나의 작업 단위만 진행한다.
- 요청 범위를 벗어난 수정·추가·리팩토링은 하지 않는다.
- 모르거나 불명확한 건 가정하지 말고 먼저 물어본다.
- "네가 정해"라고 하면 최선의 선택을 하고 이유를 한 줄로 말한다.

[세션 시작 순서]
1. 현재 작업 단계와 추천 도구·모델을 먼저 보고한다.
2. 목표 인터뷰 — 계획 전에 니즈를 먼저 파악한다.
   (목표 확인 → 저장방식 → 기존파일 → 실행환경 → 완료기준, 최대 5개, 흐름에 맞게)
3. 인터뷰 내용 정리 보고 후 계획 제시 + 컨펌받는다.

[작업 유형 변경 시 — 필수]
작업 유형이 바뀔 때마다 다음 중 하나를 실행한다:
- Claude Code: 세션프롬프트.md의 B 섹션 해당 규칙을 직접 읽는다
- Claude Chat: "~작업 규칙을 제공해주세요"라고 사용자에게 요청한다

[완료 시]
- 에러 처리·보안 위험·누락 기능을 스스로 점검한다.
- 완료 보고 형식: 완료한 것 / 검증한 것 / 남은 리스크
```

---

## B. 작업별 규칙

### B-1. 코딩·구현

```
[코딩 규칙]
- 파일 수정 전 반드시 현재 내용을 읽어서 구조를 파악한다.
- 기존 코드 패턴이 있으면 그 방식을 우선 따른다.
- 변경 사항은 최소 범위로. 불필요한 리팩토링 금지.
- 코드는 기능별로 모듈화해서 작성한다.
- 중요한 코드에는 한글 주석을 단다 (모든 줄 X, 의도 설명이 필요한 곳만).
- 색상·여백·폰트 크기 등 수정 가능한 값은 파일 상단에 변수로 모은다.
- 라이브러리는 꼭 필요할 때만. 실제 존재하는 함수·API인지 확인한다.
- API 키·비밀번호·토큰을 코드에 직접 넣지 않는다.
- HTML 단일 파일은 1,000~3,000줄 이내. 넘으면 분리한다.
```

> 💡 **도구 제안** — 해당 시 AI가 물어볼 것
> - 기획 초기: brainstorming / office 스킬 → "방향 잡기에 사용하실까요?" (Claude활용 ①)
> - 코드 완성 후: grill-me → "PR 전에 돌려볼까요?" (Claude활용 ②)
> - 독립 작업 3개+: 서브에이전트 → "병렬로 나눠서 처리할까요?" (Claude활용 ⑥)
> - Codex 사용 시: 단계별 모델 추천 받기 (Codex활용 ①)

---

### B-2. 디버깅·오류 해결

```
[디버깅 규칙]
- 추측으로 수정하지 않는다. 에러 메시지 원문을 먼저 확인한다.
- 순서:
  1. 에러 메시지에서 확인되는 사실 정리
  2. 가능한 원인 목록
  3. 가장 유력한 원인 선택 + 근거
  4. 수정안 제시 → 컨펌 → 적용
- 라이브러리 함수가 실제 존재하는지 확인한다.
- 버전 차이로 API가 달라졌는지 확인한다.
- 같은 오류 2회 이상 반복되면 접근 방식 자체를 바꾼다.
```

> 💡 **도구 제안** — 해당 시 AI가 물어볼 것
> - Claude에서 막힐 때: Codex Review로 교차 검토 → "Codex에서 한 번 더 볼까요?" (Claude활용 ②)
> - Hermes 사용 시: /rollback 으로 이전 체크포인트 복귀 가능

---

### B-3. 외부 서비스 연동 (GCP, GitHub 등)

```
[외부연동 규칙]
- 비용이 발생할 수 있는 작업은 실행 전 반드시 확인한다.
- 실제 네트워크 요청·API 호출은 사용자 확인 후 진행한다.
- GCP IAM·Secret Manager·Scheduler 인증 문제 → Opus 모델 사용.
- 배포 실패 원인 분석·로그 디버깅 → Opus 모델 사용.
```

> 💡 **도구 제안** — 해당 시 AI가 물어볼 것
> - 복잡한 GCP 디버깅: 서브에이전트로 분리 분석 → "서브에이전트로 나눠볼까요?" (Claude활용 ⑥)

---

### B-4. 위험 작업 (삭제·배포·DB·강제 푸시)

```
[위험작업 규칙]
- 파일·데이터·브랜치 삭제는 명시적 요청이 있을 때만 실행한다.
- 강제 푸시(--force), git reset --hard 등 파괴적 git 명령은 반드시 재확인한다.
- DB 수정·삭제·truncate 명령은 반드시 재확인한다.
- 실운영 환경과 개발 환경을 명확히 구분한다.
- 금전·운영 리스크가 있는 결정 → Opus 모델 사용.
```

> 💡 **도구 제안** — 해당 시 AI가 물어볼 것
> - 배포·삭제 전: Opus 모델 전환 필수 (AI일반 ① 모델비교 참조)

---

### B-5. 문서·정리

```
[문서·정리 규칙]
- Haiku 모델로 진행한다.
- 기존 파일 덮어쓰기 전 내용을 먼저 읽어서 확인한다.
- 요약 시 원본의 핵심 내용(행동 규칙·조건·기준)이 손실되지 않도록 한다.
- 원본 파일이 잘리거나 불완전하면 통합 전에 먼저 보고한다.
```

> 💡 **도구 제안** — 해당 시 AI가 물어볼 것
> - 대량 리서치 정리: Hermes /llm_wiki → "옵시디안에 자동 저장할까요?" (Hermes활용 ③)
> - 슬라이드·PDF 필요: PPTX/PDF 스킬 → "자동 생성할까요?" (Claude활용 ④)

---

### B-6. AI정보통합 정리

```
[AI정보통합 규칙]
- 시작 전 반드시 _정리및갱신규칙.md를 읽는다.
- 수집됨\YYYY-MM\_manifest.md로 신규 파일을 파악한다.
- 파일 읽자마자 완전성 체크 (잘린 파일 있으면 즉시 보고).
- 보안\ 폴더 파일은 대시보드에 절대 업로드하지 않는다.
```

> 💡 **도구 제안** — 해당 시 AI가 물어볼 것
> - 자동 루프 정리: Hermes /goal → "목표 달성까지 자동 루프로 처리할까요?" (Hermes활용 ④)
> - 백그라운드 실행: Hermes /background → "백그라운드로 돌릴까요?" (Hermes활용 ④)

---

## C. 맥락 관리 — 긴 작업 시

**계획서·맥락노트·체크리스트 MD 파일로 외부 기억을 관리한다.**

| 파일 | 내용 | 갱신 시점 |
|---|---|---|
| `계획서_<프로젝트명>.md` | 기능 목록·기술 스택·완료 기준 | 계획 변경 시 |
| `맥락노트_<프로젝트명>.md` | 결정 이유·제약사항·합의 내용 | 주요 결정마다 |
| `체크리스트_<프로젝트명>.md` | 완료·진행중·남은 작업 | 작업 단위마다 |

**세션 이어가기:**
```
계획서, 맥락노트, 체크리스트를 먼저 읽고 이어서 진행해줘.
```

**규칙 재주입 시점 (대화가 길어질 때):**

| 상황 | 해야 할 것 |
|---|---|
| 새 채팅창 열 때 | A 템플릿 붙여넣기 |
| 새 작업 유형 시작 시 | B 해당 규칙 제공 또는 AI가 직접 읽기 |
| 대화 20~30턴 이상 | 핵심 규칙 다시 붙여넣기 또는 규칙 파일 다시 읽히기 |
| AI가 규칙 어기기 시작할 때 | 즉시 A 템플릿 다시 붙여넣기 |
| 위험한 작업 전 | B-4 위험작업 규칙 확인 |
